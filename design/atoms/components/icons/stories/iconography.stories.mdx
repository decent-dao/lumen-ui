import { Meta, Story, Canvas } from "@storybook/addon-docs"
import { SupportQuestion } from "../src"
import { ATOMS } from "../../../../constants"
import { DESIGN_TOKENS } from "../../../../constants"
import { DisplayIconography } from "./components/DisplayIconography"

<Meta
  title={`Atoms/Icons`}
  component={DisplayIconography}
  parameters={{
    viewMode: "docs",
  }}
/>

## Usage

All Chakra-UI properties will be available, such as `boxSize` and `color`. as well as any spacing options.

```ts
import { SupportQuestion } from "@decent-org/lumen-ui"
```

<Canvas>
  <Story name="Usage">
    <SupportQuestion boxSize="1.5rem" color="lumen-blue.400" />
  </Story>
</Canvas>

## Icons

Use the name shown for the icon to import from `@decent-org/lumen-ui`

<Story name="Icons">
  <DisplayIconography />
</Story>
